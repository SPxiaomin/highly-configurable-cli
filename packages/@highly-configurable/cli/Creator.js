
module.exports = class Creator {
  constructor(name, context) {
    this.name = name;
    this.context = context;
  }

  async create(options) {
    const {
      name,
      context,
    } = this;

    // devDependences

    // template
  }
}
